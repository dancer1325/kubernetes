# apimachinery

* == Kubernetes objects' & Kubernetes-like API objects' 
  * scheme packages
  * typing packages
  * encoding packages
  * decoding packages
  * conversion packages
* use cases
  * enable servers & clients / can work -- , WITHOUT direct type dependencies, with -- Kubernetes API infrastructure 
* uses
  * `k8s.io/kubernetes` + `k8s.io/client-go` + `k8s.io/apiserver`
